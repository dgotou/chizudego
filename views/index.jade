extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/form", target="new") New Entry
  list = ['a','b','c']
  ul
   for item, index in list
    if(index % 2) == 0
     li.even= item
    else
     li.odd= item
