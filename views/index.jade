extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/form", target="new") New Entry
  list = ['a','b','c']
  ul
   for item, index in items
    if(index % 2) == 0
     li.even= item.text
    else
     li.odd= item.text
